Nexarvia Technologies Integration Test Report
Build: V13 live tracking and referral analytics

Implemented:
- Landing page and admin use the same Firebase configuration.
- Live visitor records include entered fieldData, status, progress, current field, consent, and referral source.
- Application, referral profile, referral code, referral join, and completion record are written atomically.
- Firebase server timestamps are used for database records.
- Referral visits, form starts, completed steps, submissions, and shares are tracked.
- Admin reads referral events and shares, shows referral visits/starts/shares/joins and live referral source.
- CSV exports separate student referral code and referred-by code.
- Data listener errors are surfaced in the dashboard system-health panel.
- Nexarvia Technologies branding is applied across landing, admin and login surfaces.

Deployment requirement:
Deploy firebase-rules.json before testing the public form.

V14 MOBILE PARITY VALIDATION — 04 Aug 2026
- Added mobile-responsive.css after all landing-page embedded styles.
- Added admin-mobile-responsive.css after dashboard.css.
- Verified mobile admin navigation includes all desktop destinations and every anchor target exists.
- Verified landing and admin viewport metadata.
- Parsed new CSS files with tinycss2: no stylesheet parse errors.
- Parsed HTML and verified linked local stylesheets exist.
- Checked admin.js, login.js and extracted landing-page inline JavaScript with Node syntax checker.
- Confirmed desktop CSS is unchanged outside max-width media overrides.
